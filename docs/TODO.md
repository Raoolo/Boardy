# Boardy TODO

Actionable backlog. When the user asks "what's next?", read top-down and propose
the highest-priority unticked item with the trade-off in 2–3 sentences.
For long-form rationale see `secondbrain/memo-boardy-future.md`.

## 🟡 Rimasto aperto da "BGG Files 2ª fonte"

- [ ] **Verifica Docker ARM con Chromium** (rimandato 2026-06-09: Docker non installato in locale + VM Oracle ARM non ancora provisionata). Quando la A1 è in piedi: `docker compose -f deploy/docker-compose.yml up -d --build` → controlla che il layer `playwright install --with-deps chromium` builda su aarch64 e che un download BGG funzioni nel container (RAM/1-OCPU). Il Dockerfile è già pronto (`PLAYWRIGHT_BROWSERS_PATH=/opt/ms-playwright`, install in runtime stage come root).
- [ ] *(nice-to-have)* Filtro relevance sui candidati 1j1ju: per giochi assenti ritorna fuzzy-junk (Elfenland→Wolfenstein) e il modello sceglie dal titolo.

## ✅ Done

- [ ] **Re-download regolamenti rimossi dal gate lingua** (2026-06-23) — la bonifica (`etl/purge_bad_lang_rulebooks.py`) ha tolto 2 manuali avvelenati: **Barrage** (era thai, e pure il gioco sbagliato — testo di «Trails») e **Vampiri: The Masquerade - Heritage** (ungherese, anch'esso pare un altro gioco). Vanno ri-scaricati in EN/IT. ⚠️ **NON fattibile dalla macchina dev locale**: il PDF base 1j1ju di Barrage è scansionato (no testo), la fonte BGG richiede Chromium/Playwright (non supportato su Ubuntu 26.04 in locale) e manca `TAVILY_API_KEY`. **Sul server invece sì**: Chromium è nel container Docker → `download_rulebook('Barrage', bgg_filepageid=182905)` (BGG "English Final Rulebook") dovrebbe funzionare; in alternativa il flow foto-OCR. Da fare al primo accesso al server. (Barrage locale ha per ora solo il riassunto EN da 1 pagina, id 48.)
- [ ] **(idea) Traduzione dei regolamenti in lingua non ammessa** (2026-06-23) — alternativa al gate "rifiuta e basta": tradurre il regolamento in EN/IT prima di indicizzarlo, così si coprirebbe QUALSIASI lingua (thai, ungherese, giapponese…). **Contro** (per cui resta gate-and-reject di default): (1) i **nomi/termini specifici del gioco** si perdono o si storpiano nella traduzione (l'utente l'ha notato); (2) costo/latency di una passata LLM su tutto il manuale; (3) la citazione di pagina si sfuoca; (4) nuovo punto di fallimento. Nota tecnica: **non è un limite dell'embedding model** — `multilingual-e5-base` legge benissimo il thai e fa retrieval cross-lingua; il vincolo vero è DeepSeek che risponde bene + l'utente che verifica. Quindi semmai la traduzione va vista come *escape hatch opzionale* solo per i giochi che NON hanno un'edizione del regolamento in una lingua ammessa (per Barrage/Vampiri invece l'edizione EN esiste → basta scaricare quella giusta). Da valutare se/quando capita un gioco senza alcuna edizione ammessa.
- [x] **Gate lingua sui regolamenti** (2026-06-23): Boardy indicizza solo manuali in lingue ben gestite da DeepSeek+e5 — **EN, IT, ES, DE, FR, PT** (override env `BOARDY_RULEBOOK_LANGS`). **Perché:** il suffisso del filename su 1j1ju mente (sito francese → un `-rules.pdf` può essere testo francese o altro), quindi `guess_lang` non basta. Soluzione: `rulebooks.detect_language(text)` sniffa la lingua REALE del testo estratto via **py3langid** (port di langid.py, deterministico, offline, 97 lingue — più robusto del detector a stopword fatto a mano, scartato; il progetto ha già dep ben più pesanti). Il gate vive in `rulebooks.ingest_bytes` → copre `download_rulebook`, `ingest_rulebook` locale e l'hook `_backfill_rulebook` (foto OCR via `ingest_pages` restano libere: l'owner sceglie fisicamente quel manuale). Un PDF in lingua non ammessa torna `{"error": ..., "detected_lang": ...}` → la chat propone un'altra edizione. **Bonifica retroattiva** del già-indicizzato: `etl/purge_bad_lang_rulebooks.py` (dry-run / `--apply`). Caso reale che l'ha motivato: **Barrage** indicizzato con un PDF **thai del gioco sbagliato** («Trails») e **Vampiri Heritage** in ungherese — entrambi rimossi.
- [x] **Fix 403 bot Telegram (chat owner)** (2026-06-23): inviare un secondo messaggio a una conversazione owner di origine Telegram dava **HTTP 403** ("cannot continue another user's conversation"). Causa: il bot si autentica sempre come account di servizio condiviso `BOARDY_BOT_USERNAME` (`boardybot`), mentre la conv Telegram è salvata con l'identità Telegram dell'owner (`actor_id`=user_id, `actor_name`=nome Telegram) per l'audit → `is_owned_by_user` non matchava né per id né per nome e bocciava ogni continuazione. Fix in `conversations.is_owned_by_user`: una conv `origin='telegram'` con `actor_role='owner'` è continuabile da qualsiasi utente con ruolo `owner`/`admin` (il bot già gatekeepa quale `conversation_id` un `chat_id` può continuare via il suo state file, e solo owner/admin arrivano a quel branch). (Il 403 non c'entrava con Tailscale/wifi: era un bug latente su ogni 2° messaggio.)
- [x] **OCR regolamenti da FOTO** (2026-06-23): l'utente fotografa le pagine di un regolamento fisico e il sistema le legge e le indicizza come gli altri. **Perché non DeepSeek:** `api.deepseek.com` è solo testo → il passo foto→testo usa **Google Gemini** (`gemini-2.5-flash`, env `GEMINI_API_KEY`/`GEMINI_OCR_MODEL`) in `app/ocr.py`; DeepSeek resta il cervello per il resto. Trascrive testo **+ descrive a parole diagrammi/icone** (così entrano nel RAG e5) e avvisa su pagine sfocate (`legibility=poor`) e **buchi** di numerazione. Pipeline: `ocr.transcribe_images` (1 call/foto, retry su 503/429) → `tools.ingest_rulebook_photos` (ordina, rileva buchi, assembla le foto in PDF con Pillow per `pdf_blob`) → `rulebooks.ingest_pages` (riusa il core di `ingest_bytes`). Schema **v10**: `rulebooks.ocr_report` (JSON avvisi). Interfacce: web — tasto 📷 accanto a Invia + drag-drop, endpoint owner-only `POST /rulebooks/upload-photos`; Telegram — foto con didascalia «regolamento di X» **o** `/regolamento <gioco>` + `/fine` (album via debounce su `media_group_id`). **Formati supportati: JPG, PNG, WebP** (max 40 foto, 15 MB/foto). Deps nuove: `google-genai`, `pillow`. Deployato (rebuild immagine). Vedi LEARNINGS 2026-06-23.
- [x] **BGG Files come 2ª fonte regolamenti** (2026-06-09): download via headless browser (Playwright + login BGG). `etl/bgg_files_api.py` (discovery JSON aperta) + `etl/bgg_browser.py` (`BGGSession` riusabile, login una volta per batch, intercetta la risposta `downloadurls` e scarica l'URL hash). Integrato in `find_rulebook` (merge 1j1ju+BGG), `download_rulebook(bgg_filepageid=...)`, hook `_backfill_rulebook` (gate `BGG_BROWSER_ENABLED`, score≥2.0), `backfill_rulebooks.py --source {1j1ju,bgg,both}`. Env `BGG_USERNAME`/`BGG_PASSWORD`. **Bug chiave risolto: `fileid` ≠ `filepageid`** — la filepage chiave sul filepageid, navigare per fileid dà 404 (vedi LEARNINGS 2026-06-09 sera). Copertura regolamenti 36 → **45/58 posseduti**. Limite residuo: PDF scansionati (Annunaki) → servirebbe OCR.
- [x] ETL: parse `1) ElencoGiochi.xlsx` → SQLite, regex-split the messy SLEEVE column.
- [x] FastAPI + Anthropic tool-use chat (Sonnet 4.6 default).
- [x] Server-side conversation persistence + dropdown switcher in UI.
- [x] Star-schema refactor (games dim + designers/publishers/categories/mechanics outrigger dims via bridges; sleeve facts).
- [x] Web search: client-side `web_search` tool (Tavily) with trusted-domain allowlist — provider-agnostic.
- [x] `add_game` / `update_game` / `delete_game` / `set_sleeve_requirements` tools with user-confirmation flow.
- [x] Rulebook RAG: pypdf parsing, local sentence-transformers embeddings, brute-force cosine search, page-cited answers.
- [x] Rulebook auto-fetch (2026-06-09): `find_rulebook` (read, 1j1ju via `etl/onejour_api` + Tavily fallback) → `download_rulebook` (write, scarica+indicizza, valida `%PDF`) → `ask_rules`. UX proponi-e-conferma. Chiude la catena find→download→ingest→ask senza più passare il PDF a mano.
- [x] Rulebook storage in DB (schema v9, 2026-06-09): PDF grezzo in `rulebooks.pdf_blob` (`ingest_bytes` core, `get_pdf` export). Backup unico/portabile. Niente più scrittura su `rulebooks/`.
- [x] Auto-fetch rulebook su nuovo gioco (2026-06-09): hook `_backfill_rulebook` post-`add_game`, scarica solo su match esatto del titolo (altrimenti la chat propone). Resolver nome tollerante in `rulebooks._resolve_game`.
- [x] Drag-and-drop PDF upload with autocomplete game picker.
- [x] BGG backfill script (`etl/backfill_bgg.py`) using Haiku 4.5.
- [x] BGG backfill v2 via official XML API2 (`etl/bgg_api.py` + `etl/backfill_v2.py`). Awaits BGG token to run.
- [x] Audit log `changes(...)` + integration in all write tools (memo §8). New tool `recent_changes` for the model.
- [x] Delta-based inventory tool `add_to_inventory(width, height, delta, brand?)`.
- [x] System prompt teaches `add_to_inventory` vs `update_inventory` and `recent_changes` for history Qs.
- [x] Pluggable LLM providers (`app/llm.py`): Anthropic + OpenAI-compatible (Ollama/DeepSeek). DeepSeek-chat is the new default — ~10× cheaper than Sonnet.
- [x] Local LLM via Ollama — **archived 2026-04-29**. Provider lives on but disabled in practice: hardware (AMD APU, shared RAM, no NPU support in Ollama) + 7B tool-use quality both insufficient. Re-open only with dGPU or a stronger small model. See `LEARNINGS.md` 2026-04-29 (PM) + `secondbrain/memo-boardy-future.md` §1.
- [x] Sleeve schema v3 (2026-04-29 PM): drop `sleeve_raw`, collapse `'no'`→`'na'`, `sleeve_requirements` reinterpreted as TODO list (rows only for non-sleeved games), cascade-clear in `update_game`, guard rule in `set_sleeve_requirements`. Idempotent migration in `app/schema.py`.
- [x] Import bug fix (2026-04-29 PM): `classify_sleeve` defaulted numeric-only Excel cells to `sleeved` — fixed to `unknown`. 5 games restored from audit log via `etl/fix_misclassified_sleeve.py`.
- [x] Tagged stdout logging of every tool-use round (`[boardy] conv=… round=… …`) for live debugging from the uvicorn terminal.
- [x] Web search reads FULL page (Tavily `raw_content` + `search_depth=advanced` defaults). Snippet was wrong on BGG/sleeveyourgames; full page text fixes it. (2026-05-01)
- [x] Counting bug: list-returning tools now wrap as `{count, items}` so the model transcribes the integer instead of estimating list length. Header/list mismatch ("28 giochi" with list of 29) eliminated. (2026-05-01)
- [x] **`/sleeves` dashboard** (2026-05-01): KPI cards, "Da comprare" table, inventory with inline +/- preset buttons (`-50/-10/+10/+50/+100`), quick-add form, mini-chat with separate `conversation_id`. New endpoints `/sleeves/data`, `/sleeves/inventory/delta`, `/sleeves/inventory/upsert` (audit-source `web:sleeves`). Library got a Buste status pill column + filter; nav `Chat / Libreria / Buste` shared across pages.
- [x] **Frontend rerender bug** (2026-05-01): `web/index.html` only rendered Anthropic-shape histories (`content` as array). DeepSeek/OpenAI shape (`content` string + separate `tool_calls`) was silently skipped → reloaded conversations showed only user bubbles. Now accepts both shapes per turn.
- [x] **Citation suffix cleanup** (2026-05-01): killed the `[↗](url)` pattern. The prompt was teaching the model to write arrow-icon link suffixes; replaced the example with normal `[Value](url)` syntax. Also dropped the dead Anthropic-citation injection in `chat.py` — Tavily-backed `web_search` makes citations the model's own prose now.
- [x] **Skip-reason column + tool surfaces excluded games** (2026-05-03 PM): schema v5 adds `games.description_skip_reason TEXT` (idempotent migration); backfill script writes it on skip/error and clears on success, so re-runs are naturally idempotent. New `--retry-skipped` CLI flag. `search_games_semantic` now returns `{count, items, excluded_count, excluded}`; tool description tells the model to MUST mention the excluded list when non-zero (anti-silent-subset). Also tightened the DeepSeek json_object prompt (single-line strings, ASCII apostrophes, escape rules) and added `_try_repair_json` fallback (curly→ASCII normalize, newline collapse) — kills the deterministic apostrophe bug observed on Memoir/War Chest.
- [x] **Semantic search on `games.description`** (2026-05-03): hybrid SQL-filter + cosine over e5-base embeddings of the BGG description. Schema v4 adds `description_embedding BLOB` + `description_hash TEXT` to `games` (idempotent). New module `app/games_semantic.py` reuses the rulebooks model. New tool `search_games_semantic(query, players?, max_complexity_weight?, max_duration_min?, sleeve_status?, category_contains?, mechanic_contains?, k=10)`. Auto-embed hook in `add_game`/`update_game` (best-effort, never breaks the write). Backfill via `etl/embed_descriptions.py` — 32/56 games have descriptions and are now indexed; the other 24 will be picked up automatically once `backfill_v2` enriches them. System prompt teaches when to pick semantic vs `list_games`.
- [x] **CLAUDE.md refactor** (2026-05-04): trimmed from ~206 → ~50 righe seguendo i principi Claude Code per CLAUDE.md. Struttura: Setup & commands → Where to look (mappa file con 1 riga ciascuno) → Companion docs (incluso `secondbrain/` con regola "non scrivere senza permesso") → Conventions → Environment. Tagliata la prosa architetturale (auto-documentata dal codice); tenute solo regole non deducibili e gotcha. Era TODO medium "Refactor CLAUDE.md", ora chiuso.
- [x] **Coverage gap chiusa via web-search backfill** (2026-05-04): nuovo `etl/backfill_descriptions_websearch.py` — Tavily allowlist allargato (BGG + Wikipedia IT/EN + publishers) + DeepSeek estrazione description-only (superficie minore della backfill multi-campo = niente bug JSON). Flag `--manual TEXT` per giochi parodia / sole edizioni IT (Tortelli, I Coloni di Catan). I 4 giochi residui (`7 Wonders II`, `I Coloni di Catan`, `Il Signore dei Tortelli -Le Due Torri-`, `War Chest`) sono ora indicizzati: `excluded_count=0` su tutto il catalogo. Catan-style query → "I Coloni di Catan" 0.812 (strong); War Chest query → "War Chest" 0.837 top hit.
- [x] **BGG backfill completo** (2026-05-05): phase1 patched 39/52, phase2 `--auto` agganciato 3/4 (Tortelli no-hits, atteso — è una parodia). Due match ambigui auto-applicati e poi corretti via `apply`: "I Coloni di Catan" → bgg=13 (Catan base, non Esploratori e Corsari) e "War Chest" → bgg=249259 (base 2018, non l'espansione Shock Tactics 2026). Coverage finale: 55/56 su `bgg_id`, `thumbnail_url`, `complexity_weight`, `bgg_rating`, `year_published`, `age_min`; 56/56 su `duration_min`; 42/56 su `image_url` (alcuni giochi BGG non hanno full-image). Sblocca grid view + logo BGG.
- [x] **Library v2 — grid view + filtri evoluti** (2026-05-05): toggle "▦ Tabella / ▥ Griglia" persistito in localStorage; card responsive `auto-fill,minmax(180px,1fr)` con `thumbnail_url` lazy-loaded, fallback 🎲 per giochi senza cover. `<colgroup>` + `table-layout: fixed` per evitare lo shift colonne ai cambi di filtro. Categorie/Meccaniche convertiti a multi-select dropdown custom (checkbox + search + click-outside, semantica OR within-filter / AND between-filters). Tag column con `+N altre` cliccabile per espandere inline le tag oltre le prime 3 — niente più righe alte 4cm. Nuovo filtro **Durata max (min)**. Bug critico chiuso: `.filters input` (selettore discendente) imponeva `min-width:140px` alle checkbox del multi-select → "spazio box spazio nome"; risolto con `.filters > input` (figlio diretto).
- [x] **Smart-filter chatbot su `/library`** (2026-05-05): nuovo endpoint `POST /library/filter` con DeepSeek (`deepseek-chat`, T=0, tool_choice forced) + tool unico `apply_filter` con schema rigido. Path ibrido: query strutturali ("da 2 facili") popolano i dropdown; query semantiche ("simili a Catan", "rilassanti") chiamano `gs.search_semantic()` con i filtri strutturali come SQL pre-filtri e tornano `semantic_ids` → frontend filtra con `state.semanticIds: Set` come quarto livello in `applyFilters()`. Banner "🔍 Filtro semantico attivo" sopra la tabella con bottone Rimuovi. UX silent: niente bubble log, dock in cima alla pagina con `[textarea | Invia | Azzera]` + status-line con stati colorati (thinking giallo pulsante, success verde, error rosso). System prompt embedda elenco categorie/meccaniche valide → niente allucinazioni; sanitizzazione anche server-side. Flag `reset` e `applied` distinguono i tre casi (azzera / filtri estratti / query ambigua → preserva stato). Costo: ~$0.0005/query.
- [x] **`/sleeves`: nascondi righe inventario a count=0** (2026-05-05): `renderInventory()` filtra `STATE.data.inventory` su `count_owned > 0` prima di costruire la tabella. Il record resta nel DB (audit + cronologia preservati); se la misura/brand torna a >0 via delta o upsert la riga riappare. `inv-meta` ora conta solo le righe visibili. Nessuna migrazione, nessun cambio backend.
- [x] **`/sleeves` status-line: strip markdown lato client** (2026-05-06): le risposte dell'assistant dopo write tools arrivavano con `**bold**` e `-` di lista literal nel dock 2-row clamp. Aggiunto helper `stripMarkdown()` in `web/sleeves.html` (regex su fenced code → inline code → `[text](url)` → emphasis → headers → bullet markers → newlines collassati a spazio). Applicato dentro `setChatStatus()` quindi è single-source-of-truth: anche eventuali messaggi futuri loaded via `loadChatConv()` passano dallo stripper. `/library` non ne ha bisogno (status strings server-side sono plain text), `/` rendera già con `marked.parse` + bubble. Niente cambiamenti al system prompt — frontend-only fix.
- [x] **Sleeves chat → silent dock in cima** (2026-05-05): `/sleeves` allineato a `/library`. Bubble log rimosso, mini-chat full-width spostata sopra `<main>`, single-row layout, status-line con thinking/success/error. Persistenza `conversation_id` mantenuta per i flussi multi-turn (es. "aggiungi 100 buste" → bot chiede dimensione → utente risponde). Successo rilevato via fingerprint pre/post `load()` su (inventory totals + summary length + to_buy length): se il fingerprint cambia → status verde, altrimenti neutro. `marked.js` ancora caricato in `<head>` ma non più usato — vedi item "rendering markdown" sotto.
- [x] **ETL upsert non distruttivo** (2026-05-04): `etl/import_excel.py` non wipea più `games` / `sleeve_requirements` / bridges. Match per `name`, UPDATE solo sulle colonne ETL-managed (players, duration, complexity_label, condition, sleeve_status); BGG-enriched (bgg_id, description, embeddings, weight, etc.) preservato di default. Bridges designer/publisher e `sleeve_requirements` ricostruiti **solo per i giochi presenti in Excel** — chat-added games (Here To Slay, Tortelli, Sherlock) sopravvivono al re-import. Stampa "Inserted N new, updated M existing, preserved K" per visibilità. Schema DDL passato a `IF NOT EXISTS` (le migrazioni v3/v4 di `app/schema.py` rimangono autorevoli). Gotcha emersa: divergenze di nome Excel↔chat (es. "Here To Slay" vs "Here To Slay, Gioco") generano duplicati — vedi LEARNINGS.
- [x] **Empty-state splash su `/` (chat home)** (2026-05-14): quando `history.length === 0` (chat nuova o appena svuotata) `#chat` non è più un riquadro vuoto. `renderSplash()` produce titolo "Cosa vuoi sapere oggi?" + sottotitolo che orienta + grid 2×2 di chip cliccabili (`Quante buste mi mancano?`, `Giochi da 2 facili e veloci`, `Consigliami qualcosa di rilassante`, `Cosa è cambiato di recente?`) che coprono i 4 verticali Boardy (sleeves / filtro strutturale / semantica / audit). Click su una chip → popola `input.value` + focus, NO auto-submit (lascia l'utente libero di editare). Stile coerente col tema scuro (`var(--bot)` + `var(--bot-edge)`, hover border-accent), responsive single-column sotto i 540px, animato con `bubble-in 280ms`. Solo `web/index.html` — niente backend.

- [x] **Riorganizzazione root + guida deploy + chiarimenti image content** (2026-05-14, tardo sera): nuove cartelle `data/` (ospita `ElencoGiochi.xlsx` rinominato da "1) ElencoGiochi.xlsx" + `boardy.db.v1.bak`) e `archive/` (`boardy-qwen.Modelfile` + `test_local.py` — legacy Ollama exploration). `etl/import_excel.py` aggiornato col nuovo path. CLAUDE.md guadagna sezione "Directory layout" in cima per mappare colpo d'occhio. Nuovo `secondbrain/memo-deploy-howto.md`: guida operativa stand-alone con setup iniziale server, update workflow, troubleshooting, cheat-sheet finale, e chiarimento esplicito di cosa NON c'è nell'image (no DB, no Excel, no .env — utile per amico paranoico). Chiarimento per future-me: il container espone già solo `127.0.0.1:8765` sull'host che lo esegue, l'amico può scegliere se usare il `cloudflared` sidecar (profile `tunnel`) o puntare la sua reverse-proxy esistente a localhost.
- [x] **Docker setup pronto per il deploy** (2026-05-14, sera): `Dockerfile` multi-stage (uv + pre-cache del modello e5, ~1.5GB image finale), `docker-compose.yml` con due servizi (`boardy` + `cloudflared` su profile `tunnel`), `.dockerignore` stretto. Bind-mount di `app/`/`etl/`/`web/` so update = `git pull && docker compose restart boardy` (5 sec, niente rebuild). `boardy.db` su named volume `boardy_db`, `rulebooks/` bind-mountato. Healthcheck su `GET /`. Sezione "Deploy / Self-host" in `CLAUDE.md` con setup CF Tunnel in 4 passi. `app/db.py` rispetta env `BOARDY_DB` per puntare al volume `/data/boardy.db` (con fallback al path legacy `<repo>/boardy.db`). Resta da fare: deploy effettivo sul server dell'amico + creazione del CF Tunnel sul dashboard + DNS hostname `boardy.<dominio>`. Smoke test sul server quando si va.
- [x] **Wishlist polish + sleeve preview + ready-to-sleeve** (2026-05-14, pomeriggio): follow-up sull'item wishlist sotto.
  - **Skip-confirmation per i wishlist write** — la propose-table-then-wait di `add_game`/`update_game` per il wishlist non ha senso (rollback = 1 click su "✗ Rimuovi"). Tool description di `add_to_wishlist` + sezione "Wishlist confirmation policy" nel system prompt (BASE + SLIM): web_search BGG → add_to_wishlist → (opzionale) set_sleeve_requirements, reply in UNA frase, niente tabella di riepilogo. Risparmia token e UX più diretta.
  - **Chat-dock su `/wishlist`** sostituisce il quick-add form. Stile statico (no bouncy animation come sleeves — fuori contesto in una card già nested). Inner wrapper + textarea full-width + scorciatoie strutturate `priority` + `€ target` accanto al pulsante Invia. Le scorciatoie vengono appese al messaggio come `[shortcut suggeriti dall'UI: priority=high, target_price=58€]` — il bot le interpreta come intent dichiarato (no riconferma). conv_id persistito su `localStorage:boardy_wishlist_conv_id`. Fingerprint-based status verde quando la chat cambia il numero/totale/distribuzione delle righe.
  - **Tool `sleeve_summary_wishlist`** + endpoint `/sleeves/data.wishlist_preview`: aggrega per-size le sleeve_requirements dei wishlist items (hypothetical demand). Nuova sezione "🎁 Buste future" su `/sleeves` con accent purple `--wish` per non confondersi col warn-orange di "Da comprare". Chip "nuova misura" sulle taglie che l'inventory non copre ancora. KPI tile condizionale.
  - **Tool `games_ready_to_sleeve`** + sezione "✂️ Pronti da sleevare" su `/sleeves` (sopra "Da comprare" — è la cosa più immediatamente actionable): owned to_sleeve games le cui `sleeve_requirements` sono coperte interamente dall'inventory. Layout card-grid (non tabella) — ogni gioco è un'unità discreta. Greedy alphabetical contention check: se due "ready" competono per la stessa misura, `has_contention=true` + banner warn ("ne sleevi solo N in sequenza") evita che l'utente cominci e si trovi a metà strada.
  - **BGG media backfill hook** — `_backfill_bgg_media(gid)` chiamato post-write da `add_game`, `update_game`, `add_to_wishlist`, `update_wishlist`. Quando `bgg_id` è settato ma `thumbnail_url`/`image_url` sono vuoti (caso ricorrente: il modello via web_search non estrae gli URL `<img>` HTML), patcha quei campi via `etl/bgg_api.fetch_thing()` (XML API deterministica, cache su disco). Spirit Island (riga di test del proprietario) backfillata manualmente con lo stesso helper.
- [x] **Wishlist** (2026-05-14): nuova pagina `/wishlist` + tool LLM completi. Decisione strutturale: una sola colonna `games.status` ('owned' | 'wishlist') invece di tabella separata — promote-to-owned è un UPDATE single-column, BGG pipeline/embedding/audit/dim bridges riusati pari pari. Migrazione idempotente v6 (`app/schema.py`: status NOT NULL default 'owned' + priority + notes_wishlist + target_price + index). Read-path fence: `list_games`, `sleeve_summary`, `search_games_semantic`, `library_data`, `games_names` ora filtrano `status='owned'` by default; `list_games(status=...)` e `search_games_semantic(status=...)` espongono opt-in 'wishlist'/'any' per cross-discovery. 5 tool nuovi (`add_to_wishlist`, `list_wishlist`, `update_wishlist`, `mark_as_owned`, `remove_from_wishlist`) + 5 endpoint REST (`/wishlist/{data,add,update,buy,remove}`) + pagina `web/wishlist.html` (grid+table toggle, chip priority 🔴🟡🟢, inline priority editor, modal di conferma Promise-based per buy/remove, BGG footer). Nav link "Wishlist" aggiunto su tutte e 4 le pagine. System prompt esteso (BASE + SLIM) per insegnare al modello il flow add→confirm→promote.
- [x] **Auth + guest mode** (2026-05-14, sera; aggiornato 2026-06-23): owner login con username/password locale (bcrypt diretto + cookie firmato itsdangerous, no passlib — incompatibile con bcrypt 5+, vedi LEARNINGS), tabella `users` migrazione v7. Modalità: (a) **guest web** = navigazione read-only + chat ephemera in `sessionStorage`; (b) **guest Telegram** = read-only ma chat persistita in `conversations` con metadata Telegram; (c) **owner** = pieno accesso in scrittura, ma sidebar conversazioni filtrata su “Mie” con vista “Audit” separata per owner/admin. Tool gating in `app/chat.py`: guest vede solo read tools (filtra `TOOLS` per `tools.WRITE_TOOLS` set esplicito), write endpoints REST protetti da `require_owner()`. CLI `etl/create_user.py {create,reset,list}` per gestire utenti. Frontend: helper condiviso `web/static/auth.js` (badge "Guest · Accedi" / "username · Esci" + `BoardyAuth.state()` cache) caricato dalle 4 pagine. Env `BOARDY_SESSION_SECRET` (obbligatorio in prod) + `BOARDY_COOKIE_SECURE=1` per HTTPS.

- [x] **Tag user-friendly via LLM** (2026-05-15, sera): le `categories`/`mechanics` BGG ("Hand Management", "Variable Player Powers", "Eurogame", ecc.) sono opache per non-geek. Risolto sostituendole nella UI `/library` con un nuovo set generato da LLM: vocabolario fisso di **19 tag italiani** (rilassante, strategico, tattico, caotico, tematico, astratto, narrativo, party, puzzle, bluff, negoziazione, competitivo, cooperativo, solitario, per-famiglie, per-bambini, deduzione, memoria, gestione-risorse) — vocabolario aperto rifiutato perché spezza la ricercabilità dei dropdown (sinonimi non matchano).
  - **Schema v8** (`app/schema.py`): `ALTER TABLE games ADD COLUMN friendly_tags TEXT` (JSON array). Migrazione idempotente.
  - **Modulo** `app/friendly_tags.py`: `VOCABULARY` tuple + `_SYSTEM_PROMPT` con vocab embeddato + esempi di mapping (es. eurogame=`strategico`+`gestione-risorse` non `tattico`). `generate_for_game(gid)` ritorna lista validata (intersezione con vocabolario, dedup, max 5) o `None`. DeepSeek `deepseek-chat` (T=0, `max_tokens=80`, `response_format=json_object`). Hardcodato cheap LLM (~$0.0002/gioco) come `/library/filter`.
  - **Hook auto-regen** in `app/tools.py:_backfill_friendly_tags(gid)` (best-effort, swallow errors) chiamato post-commit dopo `_backfill_bgg_media` nei 4 call site (`add_game`, `update_game`, `add_to_wishlist`, `update_wishlist`).
  - **Script batch** `etl/generate_friendly_tags.py` con `--force` / `--only NAME` / `--dry-run`. Backfill iniziale: 57/57 ok, costo ~$0.011 totali.
  - **UI `/library.html`**: rimossi dropdown `ms-category` + `ms-mechanic`, aggiunto `ms-friendly`. Tabella: rimosse colonne "Categorie" + "Meccaniche", aggiunta singola colonna "Tag". Grid view mostra friendly_tags in fondo alla card. State: `selected: {category,mechanic}` → `selected: {friendly}`. CSS: `c-cats` + `c-mechs` → `c-friendly` (width auto).
  - **Smart-filter chatbot** `/library/filter`: `_FILTER_TOOL_SCHEMA` accetta ora `friendly_tags` invece di `categories`+`mechanics`. `_build_filter_system_prompt` riceve vocabolario fisso e contiene esempi di mapping naturale→tag ("rilassanti"→`rilassante`, "in coop"→`cooperativo`). Validation server-side: filtra contro vocab set. Semantic search drop il pre-filter cat/mech (il description-embedding cattura il genere ugualmente). Smoke test: "rilassante coppia"→{players:2, friendly_tags:[rilassante]}; "cooperativi facili"→{friendly_tags:[cooperativo], complexity_max_tier:2}; "puzzle per 2 facile"→{players:2, friendly_tags:[puzzle], complexity_max_tier:2}; "simile a Catan"→20 semantic_ids. Le raw `categories`/`mechanics` restano nel DB (`/library/data` non le surface ma `search_games_semantic` può ancora pre-filtrarle se serve in futuro).
  - **Verifica qualità tag**: Azul→`astratto,puzzle,strategico`; Secret Hitler→`bluff,deduzione,negoziazione,caotico`; Wingspan→`strategico,gestione-risorse,solitario,tematico`; Sushi Go Party→`party,strategico,per-famiglie`. Spirit Island→`cooperativo,tematico,strategico,gestione-risorse`. Tutti pertinenti.

- [x] **Riassunto LLM del titolo conversazione** (2026-05-15): `_title_from_history` ora prova prima una chiamata DeepSeek `deepseek-chat` (T=0, `max_tokens=20`) sul primo scambio user+assistant; fallback alla truncation di prima se manca `DEEPSEEK_API_KEY` o l'API risponde male. System prompt focalizzato sull'INTENTO della domanda dell'utente (non sui numeri della risposta) e con regola esplicita di matchare la lingua dell'utente (IT↔EN). `save_conversation` ora query-ata sul title esistente prima: se già impostato salta del tutto il compute, evitando chiamate LLM ridondanti su ogni save successivo (il `COALESCE(NULLIF(title,''), ?)` resta come safety net). Test smoke: "quante buste mi mancano?" → "Calcolo buste mancanti", "what games for 4P in 30min?" → "Quick 4-player games", "consigliami rilassante per coppia" → "Gioco rilassante in coppia". Costo ~$0.0001/conv, calcolato una sola volta a vita per conversazione.

- [x] **"Powered by BGG" logo — footer compliance** (2026-05-10): requisito mail BGG XML API del 2026-05-04 per app public-facing. Aggiunto mount `StaticFiles` su `/static` (`app/main.py`, con `STATIC_DIR.mkdir(exist_ok=True)`), asset SVG ufficiale (`web/static/powered-by-bgg-rgb.svg`, scaricato da `boardgamegeek.com/using_the_xml_api` → "Public facing Applications"), e markup `<footer class="bgg-attr">` con link a `https://boardgamegeek.com/` su `/`, `/library`, `/sleeves`. Iterazioni UI (3 round con utente): partito centered/h=22px/opacity=0.75/border-top → troppo peso visivo per un elemento di sola compliance; finito con whisper-quiet **right-aligned**, `padding: 8px 24px`, `opacity: 0.55` (→1 on hover), `height: 16px`, no border, bg trasparente. Compliance OK (logo visibile + leggibile + linkato), peso visivo minimo. Test visivo confermato sulle 3 pagine.

## 🔴 High priority
- [ ] **Rendere Boardy accessibile da FUORI la tailnet** (richiesto 2026-06-21, dopo il deploy Docker su surfacesrvr). Oggi gira in container su `surfacesrvr` ed è raggiungibile **solo dentro Tailscale** (`http://100.73.128.40:8765`): chi non ha Tailscale installato non lo vede. Servono accesso pubblico. Due strade (NON servono port-forward sul router con nessuna delle due):
  - **(a) Cloudflare Tunnel** — *è "il tunnel"* di cui si parlava: il `deploy/docker-compose.yml` ha già il servizio `cloudflared` (profilo `tunnel`). Serve: un dominio gestito su Cloudflare + creare il tunnel sulla dashboard CF (Zero Trust → Networks → Tunnels) → copiare il **`CF_TUNNEL_TOKEN`** in `.env` → Public Hostname `boardy.<dominio>` → `http://boardy:8765` → `docker compose -f deploy/docker-compose.yml --profile tunnel up -d`. Dà un dominio personalizzato + TLS gestito da CF. Vedi sezione "Deploy / Self-host" in CLAUDE.md.
  - **(b) Tailscale Funnel** — più semplice e senza dominio proprio: espone 1 sito pubblico su un URL `https://surfacesrvr.<tailnet>.ts.net`. HTTPS Certificates già abilitati (vedi nota vault [[hosting-progetti]]); basta `tailscale funnel --bg 8765` (il primo lancio stampa un link da cliccare per abilitarlo). Zero config CF, ma URL meno "bello" e 1 solo sito pubblico per volta.
  - ⚠️ **Boardy diventa pubblico → l'auth conta davvero.** Verificare prima: `BOARDY_SESSION_SECRET` impostato (c'è) e mettere **`BOARDY_COOKIE_SECURE=1`** in `.env` (cookie solo-HTTPS). I write sono già gated owner, i guest sono read-only.
  - Decisione da prendere: abbiamo un dominio da puntare su Cloudflare? Se sì → (a). Se vogliamo provare subito senza dominio → (b) Funnel. Collegato agli item self-host più sotto (erano pensati per VM Oracle/server amico; qui il server è surfacesrvr).

## 🟡 Medium priority
- [x] **`import_excel.py` azzera `sleeve_status` corretto a mano → torna `unknown`** ✅ FATTO 2026-06-23 (fix (a)+(b): la UPDATE dei giochi esistenti non tocca più `sleeve_status` e non ricostruisce `sleeve_requirements` — entrambi ora di proprietà del DB; i giochi NUOVI li prendono ancora dall'Excel. Inoltre **guard one-time bootstrap**: lo script si rifiuta su DB già popolato senza `--force`. Nessun trigger automatico esisteva: gli `unknown` venivano da re-run manuali passati.) — *root cause* del "perché tanti giochi sono `unknown` se pensavo di aver già controllato". Distribuzione attuale (owned): sleeved 25, **unknown 21**, na 9, to_sleeve 3. L'audit mostra 19 modifiche storiche a `sleeve_status` (→to_sleeve 10, →na 8, →sleeved 1): le verifiche **erano state fatte**. Ma `etl/import_excel.py` tratta `sleeve_status` come colonna ETL-managed e a ogni re-import fa `UPDATE ... sleeve_status=?` col valore dedotto da `classify_sleeve(cella_excel)` (riga ~275); per celle ambigue/vuote → `unknown`. Quindi ogni re-import **sovrascrive le correzioni fatte via chat** (14 dei 21 unknown erano già stati toccati nell'audit). Fix possibili: (a) togliere `sleeve_status` dalle colonne sovrascritte in UPDATE (settarlo solo all'INSERT, mai aggiornarlo su righe esistenti) — il più sicuro, il DB diventa fonte di verità per lo stato buste; (b) preservare il valore DB se già diverso da `unknown` (aggiorna solo se DB=unknown ed Excel ha info migliore); (c) smettere proprio di importare quel campo da Excel. Preferenza: (a) o (b). ⚠️ **Da chiudere PRIMA del "Check DAL VIVO" qui sotto**, altrimenti il giro fisico viene perso al primo re-import. Vedi anche bugfix 2026-04-29 (classify_sleeve default→unknown) nel Done.
- [ ] **Check DAL VIVO dei giochi già sleevati** (richiesto 2026-06-21) — task operativo, non di codice: fare un giro fisico della collezione e verificare per ogni gioco se è **già imbustato o no**, poi allineare il DB (`games.sleeve_status`: `sleeved` / `to_sleeve` / `na`). Oggi lo stato potrebbe essere disallineato dalla realtà (import iniziale + stime), e tutta la dashboard Buste (Pronti da sleevare, Da comprare, Giochi futuri) si fonda su quel campo: se è sbagliato, i conti delle buste mancanti sono sbagliati. Modo pratico: scorrere `/library` filtrando per stato buste, prendere in mano i giochi e correggere via chat ("segna X come già sleevato") o UI. Bonus: tenere una check-list dei giochi controllati per non rifare due volte lo stesso. ⚠️ Prerequisito: chiudere prima l'item sopra (l'import azzera le correzioni), altrimenti il lavoro va perso.

- [ ] **Host su Oracle Cloud Free Tier (Always Free ARM)** — alternativa "spesa 0" al server fisico / Hetzner per esporre Boardy (+ un secondo sito) pubblicamente. Decisione presa il 2026-05-29: si parte da qui perché è gratis per sempre, non solo il primo mese. Chiarimenti chiave da non dimenticare:
  - **Tre livelli Oracle distinti** (fonte dell'equivoco "gratis 1 mese poi paghi"): (1) **Free Trial** = 300$ di credito da spendere in 30gg, *scade*; (2) **Always Free** = risorse gratis *per sempre*, include la ARM Ampere A1 (fino a 4 OCPU + 24GB RAM totali) — **questo è il target**; (3) Pay As You Go = a consumo. Usando solo Always Free si paga **0€ a tempo indeterminato** anche dopo il primo mese.
  - **Carta richiesta** alla registrazione (NO prepagata) solo per verifica identità: pre-auth di ~1€ rimborsata, non è una ricarica. Resta on-file per eventuali sforamenti → impostare **budget alert a 0€/1€** subito.
  - **Catch noti**: (a) `Out of host capacity` frequentissimo sulla ARM — è il motivo dello script sotto; (b) reclaim delle istanze idle su account solo-free (mitigazione: upgrade a PAYG restando dentro i limiti free = niente reclaim, sempre 0€); (c) sforare i limiti = addebito vero sulla carta.
  - ~~**Lavoro tecnico vero**: rebuild dell'immagine Docker per ARM~~ ✅ **FATTO 2026-06-05.** Nessuna modifica `platform` serve: il build avviene nativo sulla VM ARM (`docker compose up -d --build` → arm64). Il problema vero era `torch`: tirava le dep CUDA/nvidia (markate `sys_platform=='linux'`, vere anche su aarch64) → su ARM le wheel CUDA non esistono e `uv sync --frozen` rompeva. Risolto puntando torch all'indice CPU di PyTorch (`pyproject.toml` + `torch` aggiunto come dep diretta + lock rigenerato → `torch 2.12.0+cpu`, zero nvidia, wheel aarch64 confermata). Vedi LEARNINGS 2026-06-05.
  - ~~**Sub-task: script auto-find capacity ARM**~~ ✅ **FATTO 2026-06-05.** `etl/oci_find_capacity.py`: loop su `oci.core.ComputeClient.launch_instance` con backoff+jitter, cicla sulle AD, ritenta su `Out of host capacity`, notifica Telegram opzionale. OCI SDK opzionale → `uv run --with oci python etl/oci_find_capacity.py --subnet ... --image ... --ssh-key ...` (param anche via env `OCI_*`).
  - **Resta da fare (esecutivo, richiede l'account):** (1) signup Oracle + carta + budget alert 0€; (2) creare la VM A1 (a mano o con lo script); (3) `apt install docker.io docker-compose-plugin` + `git clone` + `.env` (genera `BOARDY_SESSION_SECRET`, `BOARDY_COOKIE_SECURE=1`); (4) CF Tunnel token + hostname; (5) `docker compose -f deploy/docker-compose.yml --profile tunnel up -d --build`; (6) `create_user.py` + transfer DB iniziale.
  - Vedi anche gli item self-host in Low priority (CF Tunnel, dominio esistente) — il flusso di deploy è lo stesso una volta che la VM ARM è in piedi.

- [ ] **Valutare se l'account `boardybot` serve davvero** (2026-06-23) — oggi il bot Telegram fa `POST /auth/login` come account di servizio dedicato `BOARDY_BOT_USERNAME=boardybot` (id 4, ruolo owner) per i messaggi degli owner. Dopo il fix del 403 (le conv Telegram owner sono continuabili da qualsiasi owner/admin) l'account dedicato potrebbe essere ridondante: il bot potrebbe loggarsi direttamente come un account owner umano già esistente (es. `raulo`). Da decidere: (a) **tenerlo** — pulito separare l'attore "bot" dagli umani nell'audit (`_source`/`actor_name` distinti), utile se più owner Telegram condividono lo stesso bot; (b) **toglierlo** — un account in meno da gestire/proteggere, ma l'audit non distingue più "scritto dal bot" da "scritto a mano da raulo sul web", e se domani vuoi che il bot scriva a nome del vero owner serve mappare `TELEGRAM_OWNER_IDS` → username Boardy. Costo del toglierlo: rimuovere `boardybot` da `users`, aggiornare `.env` (`BOARDY_BOT_USERNAME`), CLAUDE.md e i memo. Mia preferenza: tenerlo finché l'audit "chi ha scritto cosa" conta; rivalutare se l'account inutilizzato dà fastidio.

- [ ] **Dev-local ↔ deploy-server convergence** — una volta trasferito lo snapshot iniziale e attivato il deploy, due DB divergono in fretta: io scrivo una conv sul localhost (test/dev), gli amici ne scrivono altre sul deploy, niente merge automatico. Da capire dopo qualche settimana d'uso quale pattern preferire:
  - (a) **Deploy = prod, locale = throwaway**: tutte le interazioni reali avvengono sul server. Il `boardy.db` locale viene rigenerato/wipato come ambiente di test ogni volta che faccio dev. Cambio routine ("non scriverci mai roba che vuoi tenere"). Più pulito modello, ma perdo l'app a portata di mano quando lavoro offline.
  - (b) **Pull on demand**: piccolo script `etl/pull_from_server.py` che fa `scp` (o `docker run --rm -v boardy_boardy_db:/data alpine cat /data/boardy.db > local.db`) del DB dal server al locale prima di una sessione di dev. Push code-only, mai DB. Pro: lavoro offline con dati freschi. Con: ricordarsi di tirare giù la versione, foot-gun se dimentico e finisco a editare un DB locale stale.
  - (c) **Tunnel SSH al DB remoto**: dev locale apre un SSH tunnel e parla al DB live del server. Pro: zero sync. Con: latency su ogni query (SQLite over network non è il suo elemento), write da dev corrompono "prod".
  - (d) **Backup periodico server → cloud / repo privato**: cron sul server che fa `sqlite3 backup` e synca su un bucket/branch. Locale legge da lì. Combina (b) con automation. Effort extra.
  Mia preferenza ipotetica: **(a)** — è il pattern "il prod è il prod". Lo sviluppo nuovo gira sempre contro un DB di test (può anche essere lo snapshot iniziale rinominato). Da decidere dopo qualche settimana di uso reale: quanto spesso scrivo dati "che voglio tenere" sul localhost? Se ~mai, (a) è perfetta. Se spesso, serve (b)/(d).

- [ ] **Tasto "interrompi richiesta" in chat** (richiesto 2026-06-21) — mentre Boardy "sta scrivendo" (typing indicator) non c'è modo di annullare una richiesta lunga/sbagliata. Da valutare: lato client il `fetch('/chat')` può essere annullato con un `AbortController` (il pulsante Invia si trasforma in ⏹ Stop durante l'attesa, al click `controller.abort()` + rimuove il typing indicator). ⚠️ Limite: annulla solo l'attesa del browser, il server continua a processare il turno fino in fondo (tool-use loop) e a salvarlo — quindi "interrompi" davvero richiederebbe anche supporto server (es. endpoint cancellabile / task con cancellation token). Decidere se basta l'abort client-side (UX immediata, semplice) o serve l'interruzione vera server-side (più lavoro).
- [ ] **Voice input** via Web Speech API (browser-native, free). Mic button next to "Invia"; Italian recognition is decent in Chrome. (memo §2)
- [x] **OCR dei regolamenti da FOTO** (2026-06-23): vedi voce dedicata in "✅ Done". (Idea originale era `pytesseract` sui PDF scansionati; realizzata invece via visione Gemini su foto inviate dall'utente — qualità molto migliore su layout a colonne/icone. Resta nice-to-have: riusare lo stesso `app/ocr.py` come fallback automatico sui PDF scansionati esistenti, es. Annunaki.)
- [ ] **Chunking for tabular rulebooks** — current line-based chunker breaks HeroQuest/TI reference tables. Try a heuristic that keeps consecutive table-like lines together.

## 🟢 Low priority
- [ ] **Filtro semantico `/library`: mostrare il "ragionamento" del filtro** (richiesto 2026-06-21) — quando il chatbot `/library/filter` applica un filtro (banner "🔍 Filtro semantico attivo" o i dropdown popolati), oggi l'utente vede *il risultato* ma non *come* ci è arrivato: quali `friendly_tags`/players/complexity ha estratto, se è andato per via strutturale o semantica (`gs.search_semantic`), su quale query semantica ha matchato. Aggiungere un tasto/affordance (es. "ℹ️ Perché questi giochi?" accanto al banner) che apre un pannellino con: i campi del `apply_filter` estratti (mostrati in chiaro, es. "2 giocatori · rilassante · complessità ≤2"), il path scelto (strutturale vs semantico), e per il semantico magari i top match con lo score. Il backend `/library/filter` già ritorna i filtri applicati + `semantic_ids`; basta esporre anche il payload `apply_filter` grezzo (e gli score della `search_semantic`) nella risposta JSON e renderlo lato frontend. Rende trasparente/debuggabile il comportamento del filtro ("perché ha incluso X?"). UX da decidere: tooltip leggero, popup floating (come il pattern proposto per "Per quali giochi"), o riga di testo sotto il banner. ~1-2h. Vedi item smart-filter chatbot nel Done (2026-05-05) e il refactor friendly_tags (2026-05-15).

- [ ] **Sidebar collassata — restyle dello stato chiuso** — l'iterazione 2026-05-14 ha provato un "rail" da 52px con solo "+", ma il risultato visivo è povero ("triangolo brutto, duro, vuoto" feedback utente). Per ora la sidebar collapsa interamente a width 0 (clean, ma niente affordance visivo per riaprirla — il toggle ☰ in topbar c'è sempre, però non è "ergonomico" come avere una scaffalatura laterale). Idee da provare: (a) **rail più curato** con icona ☰ in cima + lista compatta di chat (solo prima lettera del titolo o emoji-bullet) cliccabile per saltare alla conv, hover espande temporaneamente; (b) **edge-hover reveal**: la sidebar collassa a 0 ma una striscia di 8px sul bordo sinistro è "hot" — hover apre temporaneamente la sidebar, mouseout la richiude (stile macOS Finder); (c) **floating mini-menu**: un piccolo widget fluttuante in alto-sinistra (icona + count chat) che apre un dropdown con la lista delle chat (no sidebar persistente). Decidere quando si ha voglia di iterare sull'UX, non prima.

- [ ] **Espandere mini-chat (`/library`, `/sleeves`, `/wishlist`) in chat completa** — caso d'uso utente: "filtra giochi 2-player rilassanti" su `/library` → vede i risultati nel grid → "okay mi ispira Villainous, dimmi di cosa si tratta?" — quel follow-up oggi non funziona perché `/library/filter` è single-shot, niente tool-use né `conversation_id`. Tre direzioni: (a) **bottone "↗ Apri in chat completa"** che appare dopo la prima interazione e crea una conv nuova su `/` con system note che porta il contesto (filtri attivi, risultati visti). Friction bassa, ~1h di lavoro; (b) **chat-in-place full** — espandere il dock in un pannello con bubble, routing intent "filter vs chat" che decide tra `/library/filter` e `/chat`. Refactor 3-5h, intent classifier ha edge cases ambigui; (c) **chip di follow-up smart** — dopo una risposta, mostra 2-3 chip generate dal contesto ("Dimmi di Villainous", "Filtra ancora più semplici") che linkano alla chat completa o triggerano azioni inline. Mia preferenza: (a). Sblocca subito senza rompere niente, e se l'A risulta scomoda nell'uso reale poi passi a (b). Servono decisioni su: come passare il contesto (system message? user message seedato?), cosa fare con il `conversation_id` del dock esistente (mergiare? buttare?), navigazione di ritorno da `/` alla pagina di partenza.

- [ ] **Keyboard shortcuts chat** — `Ctrl+K` nuova chat (clear input + `setConvId(null)` + rerender), `Ctrl+/` focus input, `Esc` per chiudere i modali (oggi `confirmDialog` lo gestisce, estendere al modal upload PDF). ~25 righe in `web/index.html`. Bonus: mini-hint in fondo alla sidebar tipo `<kbd>Ctrl+K</kbd> nuova chat` per renderle scopribili. Power-user feel a costo zero.
- [ ] **Sidebar: search bar conversazioni** — filtro inline sopra la conv-list che filtra `convsCache` per `c.title.toLowerCase().includes(query)`. Senso solo quando le chat sono 20+; per ora con 4-5 conversazioni è prematuro. Da riprendere se il volume cresce. Implementazione: input testuale dentro `.sidebar-header`, listener `input` che ri-chiama `renderConvList()` con un `state.filter` letto dentro la funzione.
- [x] **Ownership e visibilità delle conversazioni — prima versione** ✅ FATTO 2026-06-23: `conversations` usa metadata `origin`, `actor_role`, `actor_id`, `actor_name`; `/conversations` default = `scope=mine`, `scope=audit` solo per ruoli `owner`/`admin`; `/conversations/{id}` e `DELETE` controllano ownership o permesso audit; `/chat` blocca la continuazione di chat altrui (`403`), quindi la vista Audit è read-only di fatto. UI: sidebar con toggle `Mie / Audit`, Audit mostra attore/origine e disabilita composer. Migrazione dati: vecchie chat senza proprietario assegnate a `raulo`; `conversation_id=30` = Telegram owner raulo, `31` = Telegram guest kawatorio. Restano miglioramenti futuri: ruolo “admin” esplicito per separare owner-operativi da audit-admin, eventuale schema normalizzato `conversation_owner_id`/`owner_kind`, e vista audit più comoda se il volume cresce.
- [ ] **`/sleeves` "Da comprare": colonna "Per quali giochi" → popup** — l'attuale colonna mostra "N giochi" cliccabile che espande la lista inline (`.games-trigger` + `.games-list` con `tr.expanded` toggle, `web/sleeves.html:377-381`). Quando espansa la lista allarga molto la colonna e sbilancia la tabella. Tre direzioni: (a) **hover-tooltip CSS** — `<span title="...">N giochi</span>`, native ma stile orribile (no theming, va e viene velocemente); (b) **popup floating al click** — piccolo card assoluto posizionato vicino al trigger, con `position: fixed` + coords da `getBoundingClientRect()`, max-width fisso (~280px) e click-outside per chiudere. Stile coerente col tema scuro. Click via tastiera supportato; (c) **modal centrato** sullo stesso pattern di `confirmDialog()` — più "pesante" visualmente ma riusa il pattern esistente. Mia preferenza: **(b)**, è il giusto medio. La colonna torna `width: auto` con solo "N giochi" come trigger, niente `.games-list` inline. Implementazione: ~40 righe (popup HTML statico in fondo alla pagina + helper JS `showGamesPopup(triggerEl, games)` che posiziona, popola, chiude su click-outside / Esc). Bonus: riusabile per altre liste lunghe in futuro.
- [ ] **Easter egg `/dado` o `/random-game`** — slash command intercettato dal frontend prima del POST a `/chat`. `/dado [N]` → bubble bot locale "🎲 X" con random tra 1 e N (default 6). `/random-game` → fetch a nuovo endpoint `/games/random` (`SELECT ... ORDER BY RANDOM() LIMIT 1`) e bubble bot con la card del gioco scelto (nome + thumbnail). Spice playful, ~30 righe totali (frontend + 1 endpoint Python). Cherry on top, niente di funzionale.
- [ ] **GDPR / privacy policy per chat persistite, incluso Telegram** (richiesto 2026-06-23) — ora le chat Telegram guest vengono persistite in `conversations`, quindi non basta più il vecchio modello “guest ephemera = zero compliance”. Da chiarire/implementare: (1) pagina `/privacy` semplice con cosa viene salvato: testo chat, timestamp, ruolo, origine, Telegram `user_id`/username quando arriva dal bot; (2) base giuridica: per Telegram probabilmente informativa + uso volontario del bot, ma verificare se serve consenso esplicito/command `/privacy`/messaggio `/start` aggiornato; (3) retention: es. cancellare guest Telegram dopo 90 giorni con `etl/purge_guest_chats.py --origin telegram --actor-role guest --older-than 90d`; (4) diritto alla cancellazione: comando Telegram `/forgetme` o `/delete_my_chats` che cancella le conversazioni con `actor_id=<user_id>` + bottone web “Cancella le mie chat” per eventuale guest cookie; (5) export/accesso: comando `/mydata` o endpoint owner/admin che esporta le chat per actor_id; (6) consenso/informativa web guest se un domani persistiamo anche le chat browser: cookie `boardy_guest_id` firmato, opt-in vero, rifiuto = path attuale `sessionStorage`; (7) policy per admin/owner: chi può leggere le chat guest e perché. Stima: prima versione 2-4h; meglio farla prima di esporre Boardy pubblicamente ad amici.
- [ ] **Self-host deploy — fase esecutiva** (2026-05-14): file Docker pronti (vedi item "Docker setup"). `.env.example` ✅ già presente in repo. **Auth pronta** ✅ (vedi item "Auth + guest mode"). Target scelto: **server dell'amico via CF Tunnel**. Prossimi passi operativi: (1) sul server amico `apt install docker.io docker-compose-plugin` + `git clone` del repo + `cp .env.example .env`; (2) **generare e settare `BOARDY_SESSION_SECRET`** in `.env` (`python -c "import secrets; print(secrets.token_urlsafe(32))"`) + impostare `BOARDY_COOKIE_SECURE=1`; (3) dashboard Cloudflare dell'amico → Zero Trust → Tunnels → crea tunnel → copia token in `.env` come `CF_TUNNEL_TOKEN`; (4) Public Hostname `boardy.<dominio>.tld` → `http://boardy:8765`; (5) `docker compose --profile tunnel up -d`; (6) `docker compose exec boardy uv run python etl/create_user.py create raulo` (e gli altri amici owner); (7) smoke test su `https://boardy.<dominio>.tld` da rete esterna — landing in guest mode, login ok, write protetti; (8) trasferimento DB iniziale via `docker cp boardy.db boardy:/data/boardy.db` (oppure ETL re-import sul server). Raspberry Pi e Hetzner restano fallback se l'amico cambia idea.
- [ ] **Esporre Boardy sotto il dominio esistente** — abbiamo già un sito/dominio personale, quindi invece di un hostname Cloudflare casuale Boardy può vivere come subdomain (es. `boardy.<dominio>` o `giochi.<dominio>`) o come path sotto reverse-proxy. Decidere: (1) subdomain dedicato con DNS A/CNAME → host self-hosted (vedi item sopra) → reverse-proxy (Caddy/nginx/Traefik) con TLS automatico (Let's Encrypt o Cloudflare-origin); (2) auth: Boardy è single-user, quindi va protetto — basic-auth davanti, oppure Cloudflare Access (gratis fino a 50 utenti, login via email magic-link/Google), oppure Tailscale-only (privato). Considerare anche se il sito esistente già ha un reverse-proxy in cui inserirsi (config riuso) o se va creato da zero. Dipende dall'item self-host.
- [x] **Telegram bot** sharing the same `/chat` endpoint (memo §3, fatto 2026-05-20; aggiornato 2026-06-23). Skip WhatsApp. Implementazione in `bot/telegram_bot.py`: thin client su `POST /chat`, allow-list owner via `TELEGRAM_OWNER_IDS` (owner→cookie auth + conv persistita, non-owner→guest read-only persistito), mapping `chat_id → {role, conversation_id}` salvato su `data/telegram_chats.json`; testo/history in `conversations`. Avvio: `uv run python -m bot.telegram_bot` o `docker compose --profile telegram up -d`. Il servizio Telegram disabilita l'healthcheck HTTP ereditato dall'immagine web.
- [ ] **UI redesign attempt #2** — minimal/Linear-inspired, not skeumorphic-2005. User vetoed the wood+parchment attempt.
- [ ] **Vendor `marked.js` locally** to enable offline-first.

## 📚 Reference
- Detailed rationale: `secondbrain/memo-boardy-future.md`
- Architectural decisions & gotchas: `LEARNINGS.md`
- Code structure: `CLAUDE.md`
